{
  "name": "backend",
  "version": "1.2.1",
  "private": true,
  "scripts": {
    "dev": "npx prisma generate && npx prisma migrate deploy && tsx watch src/server.ts",
    "build": "npx prisma generate && tsc",
    "start": "node dist/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "prisma:migrate:dev": "prisma migrate dev",
    "audit": "tsx src/cli/audit.ts",
    "postinstall": "node ../scripts/patch-puppeteer.js"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/static": "^7.0.4",
    "@prisma/client": "^5.22.0",
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1",
    "jpegoptim-bin": "file:../scripts/bin-stubs/jpegoptim-bin",
    "jpegtran-bin": "file:../scripts/bin-stubs/jpegtran-bin",
    "optipng-bin": "file:../scripts/bin-stubs/optipng-bin",
    "yellowlabtools": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "overrides": {
    "optipng-bin": "$optipng-bin",
    "jpegtran-bin": "$jpegtran-bin",
    "jpegoptim-bin": "$jpegoptim-bin"
  }
}
